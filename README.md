# Systeme de placement Cegep - Frontend

Frontend React separe pour le systeme de placement en ligne.

## Installation

```bash
npm install
npm run dev
```

## Configuration

Copier `.env.example` vers `.env` et ajuster l'URL de l'API backend au besoin.
